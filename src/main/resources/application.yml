server:
  port: 1236

spring:
  application:
    name: MindAiSpringBoot
  datasource:
    url: jdbc:mysql://localhost:3306/mindai?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root
